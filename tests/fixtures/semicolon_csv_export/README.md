CSV fixture used to verify that the profiling engine can read semicolon
separated SPSTi-style exports with many columns.

This fixture is intentionally kept out of `mapping_poc_exports` so the stable
POC mapping scenario stays limited to its four canonical synthetic exports.
