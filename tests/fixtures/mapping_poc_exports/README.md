Synthetic multi-file SPSTi export used as the stable acceptance fixture for the
MCD-ST mapping engine.

The data comes from the validated mapping POC scenario, but lives outside the
prototype scratch directory so engine tests do not depend on generated files.

This directory contains the canonical synthetic CSV exports used by the POC
mapping scenario. Parser-specific fixtures live in sibling fixture directories.
